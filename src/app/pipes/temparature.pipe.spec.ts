import { TemparaturePipe } from './temparature.pipe';

describe('TemparaturePipe', () => {
  it('create an instance', () => {
    const pipe = new TemparaturePipe();
    expect(pipe).toBeTruthy();
  });
});
